
----------------------------------------

compile environment: visual stdio 2005
by chuaiGM

----------------------------------------
2014-11-23
整理环境，建立新的OpenGL2d框架
----------------------------------------

2015-10-28
由于多线程问题，客户端有些情况还是不能看到玩家列表，需要把n_NameAll变量也放到gData中，赋予volatile属性

2016-6-2
1. 待增加小蜗牛计时器
2. 待增加网络断开处理，相关提示(V)
3. 加入键盘控制（V）

网络断开处理，思路
1. 对于服务器端监测客户端断开，新写一个m_lpServerStatusProc类型回调函数，在网络类中，客户端在断开时，
获取S+IP+D+客户号，然后，从n_NameAll中，移除这个client的内容。绘图类不需进行处理。（也可以考虑加入服务器弹窗提示）
2. 对于客户端监测服务端断开，新写一个m_lpClientStatusProc类型回调函数，在网络类中，服务器断开时，
获取CD，然后，从n_NameAll中，移除第一行服务器信息，再写入一个特定信息，比如exit，然后绘图时，不断判断第一行是否为exit，
若是，着进行弹窗提示，并注销网络，返回主菜单。

2016-6-4
1. 发现当用户名配置超长时，有不稳定发生的错误，在点击联网游戏时
2. 使用const_cast转换namelist还是有问题，需要修改。
3. 客户端处理自身的退出不正常

2016-6-5
1. 使用const_cast转换namelist已经全部改为指针强转，是否再有问题，待日后验证。目前看是没有
2. 客户端自身退出不正常问题，终于找到原因，在于resetGameData函数中，也执行了网络的close操作。
   目前删除了按钮中的网络操作，都使用resetGameData复位函数中的关闭网络操作。这里可能还有一个风格问题需要优化。
   就是进入一个状态前复位，还是退出一个状态后复位，目前的原则是转为GAME_MENU状态后，都跟一个复位，
   某些进入状态也加了复位
   
TODO list
1. 加入背景音乐
2. 可以加入头像选择，建立头像库
3. 可以加入视角旋转，考虑加入3D的棋盘
4. 加入倒计时器，60秒，如果玩家没有做出动作，将执行棋子随机走动(V)

bug with miao：客户端提前按退出游戏，弹出窗口，服务器再退出

2016-6-15
TODO:
1. 在右侧画一个问号和圆圈，鼠标移动到上面，可以显示帮助信息
2. 网络对战加入版本校验，可以利用编译日期
3. 考虑加入胜利烟花特效
DONE:
键盘控制功能全部完成，用着还不错。


